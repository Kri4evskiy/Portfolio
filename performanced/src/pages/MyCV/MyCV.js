import React from 'react'

import classes from './MyCV.module.scss'

const MyCV = () => {
    const {MyCV} = classes
    
    return (
        <h1 className={MyCV}>My CV</h1>
    )
}

export default MyCV